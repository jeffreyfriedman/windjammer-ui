# Rust/Cargo
target/
Cargo.lock
**/*.rs.bk
*.pdb

# Generated Windjammer code
# Note: src/components/generated/ is now committed to support cargo publish
# (Cargo doesn't allow build.rs to modify source directory during packaging)
build_components/
build_*/
build/*.rs
build/*.rs.map
build/Cargo.toml

# Exclude nested directories created by disabled lib.rs
src/components/generated/components/
src/components/generated/lib/
# Note: platform/ is hand-written, not generated, so it's NOT excluded

# Exclude hand-written files incorrectly copied by compiler
src/components/generated/app.rs
src/components/generated/examples_wasm.rs

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Test outputs
*.log
.wasm-pack-cache/

# Temporary files
*.tmp
tmp/
.pr-comments/

# Keep root directory clean - no test/example files in root
/*.wj
/*.html
/*_test.*
/*_demo.*
/*_example.*
/serve_*
/simple_*
/test_*
